{
  "buildCommand": "pip install -r requirements.txt",
  "outputDirectory": "public",
  "framework": "python",
  "functions": {
    "api/app.py": {
      "memory": 3008,
      "maxDuration": 30
    }
  },
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/api/app"
    }
  ]
}
